const swiper = new Swiper('.swiper-container', {
    // цикличность
    loop: true,
  
    // Кнопки навигации
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },

  });